name: CI/CD React App

on:
  push:
    branches: [master]
  pull_request:
    branches: [master]

permissions:
  contents: write  

jobs:
  build-and-deploy:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Set up Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 18

      - name: Install dependencies
        run: npm ci

      - name: Run tests
        run: npm test --if-present

      - name: Build the project
        run: npm run build

      # Configure Git identity
      - name: Configure Git
        run: |
          git config --global user.name "ChakraVaishnav"
<<<<<<< HEAD
          git config --global user.email "guntakachakravaishnavreddy@gmail.com"
=======
          git config --global user.email "guntakachakravaishnav@gmail.com"
>>>>>>> 26896c8e70d018e1c03775b5f0f18997db9c9325

      # Deploy to GitHub Pages
      - name: Deploy to GitHub Pages
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: |
          git config --global user.name "ChakraVaishnav"
          git config --global user.email "guntakachakravaishnavreddy@gmail.com"
          npm install -g gh-pages
<<<<<<< HEAD
          gh-pages -d dist -r https://x-access-token:${GITHUB_TOKEN}@github.com/ChakraVaishnav/S219_CICD.git
=======
          gh-pages -d dist -r https://x-access-token:${GITHUB_TOKEN}@github.com/ChakraVaishnav/S219_CICD.git
>>>>>>> 26896c8e70d018e1c03775b5f0f18997db9c9325
